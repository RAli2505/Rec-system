# MARS — pinned dependencies for paper-result reproduction.
# Tested on Python 3.12.2 (Windows 11 Pro, NVIDIA RTX 5050 Laptop GPU,
# CUDA 12.8, driver 573.13).

# ── Core scientific stack ─────────────────────────────────────────
numpy==1.26.4
pandas==3.0.1
scipy==1.17.1
scikit-learn==1.8.0
matplotlib==3.10.8
seaborn==0.13.2
tqdm==4.67.3

# ── Deep learning (CUDA 12.8 build) ───────────────────────────────
# Install with the official PyTorch wheel index:
#   pip install torch==2.11.0+cu128 torchvision==0.26.0+cu128 \
#               torchaudio==2.11.0+cu128 \
#               --index-url https://download.pytorch.org/whl/cu128
torch==2.11.0+cu128
torchvision==0.26.0+cu128
torchaudio==2.11.0+cu128
torch-geometric==2.7.0

# ── Knowledge tracing / IRT ───────────────────────────────────────
py-irt==0.1.1

# ── Recommendation pipeline ───────────────────────────────────────
sentence-transformers==5.3.0
faiss-cpu==1.13.2

# ── Graph / KG ────────────────────────────────────────────────────
networkx==3.6.1

# ── Confidence agent (XGBoost rule-based fallback) ────────────────
xgboost==3.2.0
lightgbm==4.6.0

# ── Optional: better ALS for CF baseline (may fail to build on
#   Windows-Python-3.12 with no prebuilt wheel; the CF-only baseline
#   falls back to TruncatedSVD automatically).
# implicit==0.7.2

# ── Stats / classical ML ──────────────────────────────────────────
imbalanced-learn==0.13.0
statsmodels==0.14.2

# ── Paper figures / artefact extraction ───────────────────────────
pypdf==6.10.2
shap==0.46.0

# ── Notebooks (optional) ──────────────────────────────────────────
jupyter==1.1.1
ipykernel==6.30.0
